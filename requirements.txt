# --- Base dependencies (always required) ---
torch>=2.0.0
torchvision
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0
pillow>=10.0.0
trimesh>=4.0.0
numpy>=1.24.0
scipy>=1.10.0
huggingface-hub>=0.20.0

# --- InstantMesh integration ---
# When USE_INSTANTMESH=true, app/pipeline/instantmesh.py imports the modules
# below. Most of them come from InstantMesh's own requirements.txt (which
# pins very specific versions of diffusers/transformers/pytorch-lightning).
# The recommended setup is:
#
#   git clone https://github.com/TencentARC/InstantMesh.git models/InstantMesh
#   pip install -r models/InstantMesh/requirements.txt
#
# That installs everything below in the exact versions InstantMesh expects.
# If you prefer to install them here directly, uncomment:
#
# pytorch-lightning==2.1.2
# diffusers==0.20.2
# transformers==4.34.1
# accelerate
# omegaconf
# einops
# rembg
# imageio[ffmpeg]
# xatlas
# plyfile
# PyMCubes
# # nvdiffrast requires CUDA toolkit; install only on GPU machines:
# # pip install git+https://github.com/NVlabs/nvdiffrast/

# --- Optional: better mesh refinement ---
# pymeshlab
